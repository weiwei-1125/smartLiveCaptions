// Caption font-size levels (multipliers applied to the base orig/trans sizes). Shared by
// the overlay (to disable A−/A+ at the ends) and main.ts (to apply + persist the choice).
export const FONT_SCALES = [0.82, 1, 1.22, 1.5];
export const DEFAULT_FONT_LEVEL = 1; // index into FONT_SCALES (1 = the original size)
